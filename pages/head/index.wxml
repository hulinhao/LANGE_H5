<template name="head">
  <view class="mainHeadView"> 
         <image src='{{url}}' class="userInfo" background-size="cover" ></image>
         <image src="/image/icon/glod.png" class="glod"></image>
         <text class="glodText">{{gold}}</text>
   </view>
</template>